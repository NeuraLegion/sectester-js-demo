# 🌟 Bright Star — Run Memory

This file is auto-generated by Bright Agent. It records what the agent learned while building, authenticating, and scanning this application so future runs can skip rediscovery. Safe to commit. Delete it to force a full fresh discovery.

_Generated: 2026-07-10T14:48:18.627Z_

## Tech Stack

- **Languages:** JavaScript, TypeScript
- **Frameworks:** NestJS

## Startup

- **Command:** `cp .env.example .env; set BRIGHT_TOKEN=dummy-boot-token in .env; docker build -t sectester-js-demo-image . ; docker run --name sectester-js-demo -p 3000:3000 -d sectester-js-demo-image`
- **Port:** 3000
- **Health check path:** `/api`

## First-Run Setup

- **Completed:** no

## Authentication

- **Has auth:** no
- _hint:_ [scan-prep-warning] Scan-prep reported success without applying or documenting any rate-limit/security-control change

## Hints

**startup**
- sectester-js-demo (NestJS) uses SQLite via MikroORM, no external DB needed. Dockerfile builds fine with node:18-alpine. Requires a .env file (copy .env.example, set BRIGHT_TOKEN to any dummy value like 'dummy-boot-token' - it's only used for optional Bright/SecTester SDK integration, not required for the app to boot). Build: docker build -t sectester-js-demo-image . ; Run: docker run --name sectester-js-demo -p 3000:3000 -d sectester-js-demo-image. App listens on port 3000, exposes routes /users (GET/POST), /users/:id, /misc/fetch, /misc/xml, /misc/weekdays, and swagger docs at /api. Verified /api, /users, /misc/weekdays all return 200.

---

<!-- BRIGHT_STAR_DATA — do not edit by hand; regenerated each run -->
```json
{
  "version": 1,
  "generatedAt": "2026-07-10T14:48:18.627Z",
  "techStack": {
    "languages": [
      "JavaScript",
      "TypeScript"
    ],
    "frameworks": [
      "NestJS"
    ],
    "databases": []
  },
  "startup": {
    "command": "cp .env.example .env; set BRIGHT_TOKEN=dummy-boot-token in .env; docker build -t sectester-js-demo-image . ; docker run --name sectester-js-demo -p 3000:3000 -d sectester-js-demo-image",
    "port": 3000,
    "prerequisites": [],
    "envVars": {},
    "healthCheckPath": "/api"
  },
  "setup": {
    "completed": false
  },
  "auth": {
    "hasAuth": false,
    "hints": [
      "[scan-prep-warning] Scan-prep reported success without applying or documenting any rate-limit/security-control change"
    ]
  },
  "hints": {
    "startup": [
      "sectester-js-demo (NestJS) uses SQLite via MikroORM, no external DB needed. Dockerfile builds fine with node:18-alpine. Requires a .env file (copy .env.example, set BRIGHT_TOKEN to any dummy value like 'dummy-boot-token' - it's only used for optional Bright/SecTester SDK integration, not required for the app to boot). Build: docker build -t sectester-js-demo-image . ; Run: docker run --name sectester-js-demo -p 3000:3000 -d sectester-js-demo-image. App listens on port 3000, exposes routes /users (GET/POST), /users/:id, /misc/fetch, /misc/xml, /misc/weekdays, and swagger docs at /api. Verified /api, /users, /misc/weekdays all return 200."
    ]
  }
}
```
<!-- BRIGHT_STAR_DATA -->
