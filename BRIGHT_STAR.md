# 🌟 Bright Star — Run Memory

This file is auto-generated by Bright Agent. It records what the agent learned while building, authenticating, and scanning this application so future runs can skip rediscovery. Safe to commit. Delete it to force a full fresh discovery.

_Generated: 2026-07-15T23:17:13.649Z_

## Tech Stack

- **Languages:** JavaScript, TypeScript
- **Frameworks:** NestJS

## Startup

- **Command:** `cp .env.example .env && docker build -t sectester-js-demo . && docker run -d --name sectester-js-demo-app -p 3000:3000 sectester-js-demo`
- **Port:** 3000
- **Health check path:** `/api`

## First-Run Setup

- **Completed:** no

## Authentication

- **Has auth:** no
- _hint:_ [scan-prep] Confirmed this NestJS app has no built-in/runtime or code-level rate limiter/auth throttle to disable, relaxed the only relevant request-filtering control (body size limit), rebuilt the app, and verified 9 rapid live POSTs to `/users` all returned HTTP 201 with no rate-limit/throttle errors.
- _hint:_ [scan-prep] Inspected the running NestJS container and runtime environment for framework/product-level security settings (`env | grep -Ei 'rate|limit|throttle|csrf|session|auth|login|captcha|lock|timeout|helmet'`) and found no DB-backed/runtime rate-limit, CSRF, session, auth, CAPTCHA, or lockout settings to disable.
- _hint:_ [scan-prep] Searched the source tree for NestJS/global middleware and security controls (`ThrottlerModule`, `@Throttle`, guards, middleware, Helmet, CSRF, sessions, CAPTCHA, lockout, rate-limit libraries) and confirmed this app does not register any code-level rate limiter or auth/login throttle.
- _hint:_ [scan-prep] Patched `src/main.ts` to relax request filtering by increasing body-parser limits from `1mb` to `50mb` and explicitly allowing large JSON, URL-encoded, and text/XML payloads, reducing request-size blocking during DAST.
- _hint:_ [scan-prep] Rebuilt the Docker image and recreated the application container so the source change is active; verified the running `dist/main.js` contains the new `50mb` limit.

## Rate Limits / Scan Prep

- [sectester-js-demo-app] `sh -lc 'node -e "const fs=require(\"fs\"); console.log(fs.readFileSync(\"dist/main.js\",\"utf8\").includes(\"50mb\") ? \"body_limit=50mb\" : \"body_limit_missing\")"'`

## Hints

**startup**
- NestJS app exposes Swagger at /api and listens on port 3000 in src/main.ts; Dockerfile builds image and runs migrations during build, but requires a .env file copied into the image.
- Successful containerized startup: cp .env.example .env && docker build -t sectester-js-demo . && docker run -d --name sectester-js-demo-app -p 3000:3000 sectester-js-demo ; verified Swagger UI at http://127.0.0.1:3000/api returns 200.

---

<!-- BRIGHT_STAR_DATA — do not edit by hand; regenerated each run -->
```json
{
  "version": 1,
  "generatedAt": "2026-07-15T23:17:13.649Z",
  "techStack": {
    "languages": [
      "JavaScript",
      "TypeScript"
    ],
    "frameworks": [
      "NestJS"
    ],
    "databases": []
  },
  "startup": {
    "command": "cp .env.example .env && docker build -t sectester-js-demo . && docker run -d --name sectester-js-demo-app -p 3000:3000 sectester-js-demo",
    "port": 3000,
    "prerequisites": [],
    "envVars": {},
    "healthCheckPath": "/api"
  },
  "setup": {
    "completed": false
  },
  "auth": {
    "hasAuth": false,
    "hints": [
      "[scan-prep] Confirmed this NestJS app has no built-in/runtime or code-level rate limiter/auth throttle to disable, relaxed the only relevant request-filtering control (body size limit), rebuilt the app, and verified 9 rapid live POSTs to `/users` all returned HTTP 201 with no rate-limit/throttle errors.",
      "[scan-prep] Inspected the running NestJS container and runtime environment for framework/product-level security settings (`env | grep -Ei 'rate|limit|throttle|csrf|session|auth|login|captcha|lock|timeout|helmet'`) and found no DB-backed/runtime rate-limit, CSRF, session, auth, CAPTCHA, or lockout settings to disable.",
      "[scan-prep] Searched the source tree for NestJS/global middleware and security controls (`ThrottlerModule`, `@Throttle`, guards, middleware, Helmet, CSRF, sessions, CAPTCHA, lockout, rate-limit libraries) and confirmed this app does not register any code-level rate limiter or auth/login throttle.",
      "[scan-prep] Patched `src/main.ts` to relax request filtering by increasing body-parser limits from `1mb` to `50mb` and explicitly allowing large JSON, URL-encoded, and text/XML payloads, reducing request-size blocking during DAST.",
      "[scan-prep] Rebuilt the Docker image and recreated the application container so the source change is active; verified the running `dist/main.js` contains the new `50mb` limit."
    ]
  },
  "limits": {
    "scanPrepReplayCommands": [
      {
        "container": "sectester-js-demo-app",
        "command": "sh -lc 'node -e \"const fs=require(\\\"fs\\\"); console.log(fs.readFileSync(\\\"dist/main.js\\\",\\\"utf8\\\").includes(\\\"50mb\\\") ? \\\"body_limit=50mb\\\" : \\\"body_limit_missing\\\")\"'"
      }
    ]
  },
  "hints": {
    "startup": [
      "NestJS app exposes Swagger at /api and listens on port 3000 in src/main.ts; Dockerfile builds image and runs migrations during build, but requires a .env file copied into the image.",
      "Successful containerized startup: cp .env.example .env && docker build -t sectester-js-demo . && docker run -d --name sectester-js-demo-app -p 3000:3000 sectester-js-demo ; verified Swagger UI at http://127.0.0.1:3000/api returns 200."
    ]
  }
}
```
<!-- BRIGHT_STAR_DATA -->
