# 🌟 Bright Star — Run Memory

This file is auto-generated by Bright Agent. It records what the agent learned while building, authenticating, and scanning this application so future runs can skip rediscovery. Safe to commit. Delete it to force a full fresh discovery.

_Generated: 2026-08-13T13:14:31.760Z_

## Tech Stack

- **Languages:** JavaScript, TypeScript
- **Frameworks:** NestJS

## Startup

- **Command:** `printf 'BRIGHT_HOSTNAME=app.brightsec.com\nBRIGHT_TOKEN=dummy-boot-token\n' > .env && docker build -t sectester-js-demo-image . && docker run --name sectester-js-demo -p 3000:3000 -d sectester-js-demo-image`
- **Port:** 3000
- **Health check path:** `/api`

## First-Run Setup

- **Completed:** no

## Authentication

- **Has auth:** no
- _hint:_ [scan-prep-warning] Scan-prep reported success without applying or documenting any rate-limit/security-control change

## Hints

**startup**

- sectester-js-demo (NestJS) builds/runs via its own Dockerfile: `docker build -t sectester-js-demo-image .` then `docker run -p 3000:3000 -d sectester-js-demo-image`. Dockerfile COPYs a .env file which is not in repo; create one with BRIGHT_HOSTNAME=app.brightsec.com and BRIGHT_TOKEN=<any dummy value> before building (BRIGHT_TOKEN not actually used by app runtime, only by test:sec suite). Migrations run automatically in Docker build (npm run migration:up against sqlite test.db). App listens on port 3000, Swagger UI at /api confirms 200 OK.

---

<!-- BRIGHT_STAR_DATA — do not edit by hand; regenerated each run -->

```json
{
  "version": 1,
  "generatedAt": "2026-08-13T13:14:31.760Z",
  "techStack": {
    "languages": ["JavaScript", "TypeScript"],
    "frameworks": ["NestJS"],
    "databases": []
  },
  "startup": {
    "command": "printf 'BRIGHT_HOSTNAME=app.brightsec.com\\nBRIGHT_TOKEN=dummy-boot-token\\n' > .env && docker build -t sectester-js-demo-image . && docker run --name sectester-js-demo -p 3000:3000 -d sectester-js-demo-image",
    "port": 3000,
    "prerequisites": [],
    "envVars": {},
    "healthCheckPath": "/api"
  },
  "setup": {
    "completed": false
  },
  "auth": {
    "hasAuth": false,
    "hints": [
      "[scan-prep-warning] Scan-prep reported success without applying or documenting any rate-limit/security-control change"
    ]
  },
  "hints": {
    "startup": [
      "sectester-js-demo (NestJS) builds/runs via its own Dockerfile: `docker build -t sectester-js-demo-image .` then `docker run -p 3000:3000 -d sectester-js-demo-image`. Dockerfile COPYs a .env file which is not in repo; create one with BRIGHT_HOSTNAME=app.brightsec.com and BRIGHT_TOKEN=<any dummy value> before building (BRIGHT_TOKEN not actually used by app runtime, only by test:sec suite). Migrations run automatically in Docker build (npm run migration:up against sqlite test.db). App listens on port 3000, Swagger UI at /api confirms 200 OK."
    ]
  }
}
```

<!-- BRIGHT_STAR_DATA -->
