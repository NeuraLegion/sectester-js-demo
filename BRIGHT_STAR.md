# 🌟 Bright Star — Run Memory

This file is auto-generated by Bright Agent. It records what the agent learned while building, authenticating, and scanning this application so future runs can skip rediscovery. Safe to commit. Delete it to force a full fresh discovery.

_Generated: 2026-07-02T06:04:37.485Z_

## Tech Stack

- **Languages:** JavaScript, TypeScript
- **Frameworks:** NestJS

## Startup

- **Command:** `docker run --name discourse-local -p 3000:3000 -d discourse-local docker rm -f sectester-js-demo >/dev/null 2>&1 || true && docker build -t sectester-js-demo . && docker run --name sectester-js-demo -p 3000:3000 -d --env NODE_ENV=production --env BRIGHT_HOSTNAME=app.brightsec.com --env BRIGHT_TOKEN=${BRIGHT_TOKEN:-placeholder} sectester-js-demo`
- **Port:** 3000
- **Docker:** yes
- **Health check path:** `/api-json`
- **Prerequisites:**
  - `cp .env.example .env`
- **Environment variables:**
  - `BRIGHT_HOSTNAME=app.brightsec.com`
  - `BRIGHT_TOKEN=`
  - `NODE_ENV=production`

## First-Run Setup

- **Completed:** no

## Authentication

- **Has auth:** no
- _hint:_ [scan-prep] No scanner-blocking rate limits or security controls were present in this NestJS app, and rapid POST verification confirmed DAST traffic is not being throttled.
- _hint:_ [scan-prep] Inspected the NestJS source (src/main.ts, app.module.ts, controllers/modules) and found no code-level rate limiter, throttler guard, CAPTCHA, CSRF middleware, session timeout, auth lockout, helmet, or IP filtering to patch.
- _hint:_ [scan-prep] Queried the running container environment and built application files to confirm there were no runtime-configured rate/throttle/login/lock/csrf/session security controls active beyond a non-blocking 1mb XML body-size parser setting.
- _hint:_ [scan-prep] Verified the live app with rapid POST requests to the real write endpoint (/users): multiple consecutive POSTs all returned HTTP 201 with no 429s and no throttle/rate-limit errors, confirming the app is already permissive for DAST scanning.

---

<!-- BRIGHT_STAR_DATA — do not edit by hand; regenerated each run -->
```json
{
  "version": 1,
  "generatedAt": "2026-07-02T06:04:37.485Z",
  "techStack": {
    "languages": [
      "JavaScript",
      "TypeScript"
    ],
    "frameworks": [
      "NestJS"
    ],
    "databases": []
  },
  "startup": {
    "command": "docker run --name discourse-local -p 3000:3000 -d discourse-local docker rm -f sectester-js-demo >/dev/null 2>&1 || true && docker build -t sectester-js-demo . && docker run --name sectester-js-demo -p 3000:3000 -d --env NODE_ENV=production --env BRIGHT_HOSTNAME=app.brightsec.com --env BRIGHT_TOKEN=${BRIGHT_TOKEN:-placeholder} sectester-js-demo",
    "port": 3000,
    "prerequisites": [
      "cp .env.example .env"
    ],
    "envVars": {
      "BRIGHT_HOSTNAME": "app.brightsec.com",
      "BRIGHT_TOKEN": "",
      "NODE_ENV": "production"
    },
    "docker": true,
    "healthCheckPath": "/api-json",
    "healthProbe": {
      "method": "GET",
      "path": "/api-json",
      "expectedStatuses": [
        200
      ]
    }
  },
  "setup": {
    "completed": false
  },
  "auth": {
    "hasAuth": false,
    "hints": [
      "[scan-prep] No scanner-blocking rate limits or security controls were present in this NestJS app, and rapid POST verification confirmed DAST traffic is not being throttled.",
      "[scan-prep] Inspected the NestJS source (src/main.ts, app.module.ts, controllers/modules) and found no code-level rate limiter, throttler guard, CAPTCHA, CSRF middleware, session timeout, auth lockout, helmet, or IP filtering to patch.",
      "[scan-prep] Queried the running container environment and built application files to confirm there were no runtime-configured rate/throttle/login/lock/csrf/session security controls active beyond a non-blocking 1mb XML body-size parser setting.",
      "[scan-prep] Verified the live app with rapid POST requests to the real write endpoint (/users): multiple consecutive POSTs all returned HTTP 201 with no 429s and no throttle/rate-limit errors, confirming the app is already permissive for DAST scanning."
    ]
  }
}
```
<!-- BRIGHT_STAR_DATA -->
