# 🌟 Bright Star — Run Memory

This file is auto-generated by Bright Agent. It records what the agent learned while building, authenticating, and scanning this application so future runs can skip rediscovery. Safe to commit. Delete it to force a full fresh discovery.

_Generated: 2026-07-07T15:05:54.869Z_

## Tech Stack

- **Languages:** JavaScript, TypeScript
- **Frameworks:** NestJS

## Startup

- **Command:** `docker rm -f sectester-js-demo >/dev/null 2>&1 || true && cp .env.example .env && docker build -t sectester-js-demo . && docker run --name sectester-js-demo -p 3000:3000 -d sectester-js-demo`
- **Port:** 3000
- **Docker:** yes
- **Health check path:** `/api`

## First-Run Setup

- **Completed:** no

## Authentication

- **Has auth:** no
- _hint:_ [scan-prep] The running NestJS app has no active rate limiting or similar scanner-blocking controls, and rapid POST verification confirmed DAST traffic is not being throttled.
- _hint:_ [scan-prep] Inspected the NestJS codebase and found no framework-level or app-level rate limiter, throttler guard, CAPTCHA, CSRF middleware, session middleware, account lockout, or bot-detection components registered in source.
- _hint:_ [scan-prep] Queried the running container environment for runtime security-control settings related to rate limiting, throttling, CSRF, sessions, cookies, CAPTCHA, lockout, and timeouts; none were present.
- _hint:_ [scan-prep] Confirmed the app is already scanner-permissive in src/main.ts with broad CORS enabled and large body-parser limits for JSON, urlencoded, and XML/text payloads.
- _hint:_ [scan-prep] Verified with 5 rapid POST requests to the real write endpoint POST /users that the app consistently returned HTTP 201 responses with no 429s or throttle-related errors.

## Hints

**startup**
- Repo is a single NestJS app with an existing Dockerfile; startup works by copying .env.example to .env, then `docker build -t sectester-js-demo . && docker run --name sectester-js-demo -p 3000:3000 -d sectester-js-demo`.
- App serves Swagger UI on host port 3000 at `/api`; a successful readiness check is `curl http://127.0.0.1:3000/api` returning HTTP 200.
- Runtime/container base is node:18-alpine from the repo Dockerfile; Docker image build runs `npm run build` and `npm run migration:up` to create SQLite test.db.

---

<!-- BRIGHT_STAR_DATA — do not edit by hand; regenerated each run -->
```json
{
  "version": 1,
  "generatedAt": "2026-07-07T15:05:54.869Z",
  "techStack": {
    "languages": [
      "JavaScript",
      "TypeScript"
    ],
    "frameworks": [
      "NestJS"
    ],
    "databases": []
  },
  "startup": {
    "command": "docker rm -f sectester-js-demo >/dev/null 2>&1 || true && cp .env.example .env && docker build -t sectester-js-demo . && docker run --name sectester-js-demo -p 3000:3000 -d sectester-js-demo",
    "port": 3000,
    "prerequisites": [],
    "envVars": {},
    "docker": true,
    "healthCheckPath": "/api"
  },
  "setup": {
    "completed": false
  },
  "auth": {
    "hasAuth": false,
    "hints": [
      "[scan-prep] The running NestJS app has no active rate limiting or similar scanner-blocking controls, and rapid POST verification confirmed DAST traffic is not being throttled.",
      "[scan-prep] Inspected the NestJS codebase and found no framework-level or app-level rate limiter, throttler guard, CAPTCHA, CSRF middleware, session middleware, account lockout, or bot-detection components registered in source.",
      "[scan-prep] Queried the running container environment for runtime security-control settings related to rate limiting, throttling, CSRF, sessions, cookies, CAPTCHA, lockout, and timeouts; none were present.",
      "[scan-prep] Confirmed the app is already scanner-permissive in src/main.ts with broad CORS enabled and large body-parser limits for JSON, urlencoded, and XML/text payloads.",
      "[scan-prep] Verified with 5 rapid POST requests to the real write endpoint POST /users that the app consistently returned HTTP 201 responses with no 429s or throttle-related errors."
    ]
  },
  "hints": {
    "startup": [
      "Repo is a single NestJS app with an existing Dockerfile; startup works by copying .env.example to .env, then `docker build -t sectester-js-demo . && docker run --name sectester-js-demo -p 3000:3000 -d sectester-js-demo`.",
      "App serves Swagger UI on host port 3000 at `/api`; a successful readiness check is `curl http://127.0.0.1:3000/api` returning HTTP 200.",
      "Runtime/container base is node:18-alpine from the repo Dockerfile; Docker image build runs `npm run build` and `npm run migration:up` to create SQLite test.db."
    ]
  }
}
```
<!-- BRIGHT_STAR_DATA -->
