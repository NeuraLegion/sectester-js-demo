# 🌟 Bright Star — Run Memory

This file is auto-generated by Bright Agent. It records what the agent learned while building, authenticating, and scanning this application so future runs can skip rediscovery. Safe to commit. Delete it to force a full fresh discovery.

_Generated: 2026-06-30T08:26:45.388Z_

## Tech Stack

- **Languages:** JavaScript, TypeScript
- **Frameworks:** NestJS

## Startup

- **Command:** `docker run --name sectester-js-demo -p 3000:3000 -d --env NODE_ENV=production sectester-js-demo`
- **Port:** 3000
- **Docker:** yes
- **Health check path:** `/api-json`
- **Health response:** The response is a valid OpenAPI JSON document describing working application endpoints rather than an error or setup/configuration page.
- **Prerequisites:**
  - `cp .env.example .env`
  - `docker build -t sectester-js-demo .`
- **Environment variables:**
  - `NODE_ENV=production`
  - `BRIGHT_HOSTNAME=app.brightsec.com`
  - `BRIGHT_TOKEN=`

## First-Run Setup

- **Completed:** no

## Authentication

- **Has auth:** no
- _hint:_ [scan-prep] No active rate-limit or related scanner-blocking security controls were present in this NestJS app, and the rebuilt app was verified to accept rapid POST requests without throttling.
- _hint:_ [scan-prep] Inspected the NestJS codebase for rate limiting and security controls across source and config files; found no throttler/rate-limit, CSRF, session, CAPTCHA, auth guard, or helmet protections implemented in code.
- _hint:_ [scan-prep] Queried the running container environment for runtime-configured security controls related to rate/limit/throttle/csrf/session/captcha/lock/login/attempt/helmet and found none configured.
- _hint:_ [scan-prep] Rebuilt and restarted the application container to ensure the running app matched the inspected source state.
- _hint:_ [scan-prep] Verified the live app with 5 rapid POST requests to the real write endpoint (/users); all returned HTTP 201 with no 429s or rate-limit/throttle errors.

---

<!-- BRIGHT_STAR_DATA — do not edit by hand; regenerated each run -->
```json
{
  "version": 1,
  "generatedAt": "2026-06-30T08:26:45.388Z",
  "techStack": {
    "languages": [
      "JavaScript",
      "TypeScript"
    ],
    "frameworks": [
      "NestJS"
    ],
    "databases": []
  },
  "startup": {
    "command": "docker run --name sectester-js-demo -p 3000:3000 -d --env NODE_ENV=production sectester-js-demo",
    "port": 3000,
    "prerequisites": [
      "cp .env.example .env",
      "docker build -t sectester-js-demo ."
    ],
    "envVars": {
      "NODE_ENV": "production",
      "BRIGHT_HOSTNAME": "app.brightsec.com",
      "BRIGHT_TOKEN": ""
    },
    "docker": true,
    "healthCheckPath": "/api-json",
    "healthCheckSummary": "The response is a valid OpenAPI JSON document describing working application endpoints rather than an error or setup/configuration page."
  },
  "setup": {
    "completed": false
  },
  "auth": {
    "hasAuth": false,
    "hints": [
      "[scan-prep] No active rate-limit or related scanner-blocking security controls were present in this NestJS app, and the rebuilt app was verified to accept rapid POST requests without throttling.",
      "[scan-prep] Inspected the NestJS codebase for rate limiting and security controls across source and config files; found no throttler/rate-limit, CSRF, session, CAPTCHA, auth guard, or helmet protections implemented in code.",
      "[scan-prep] Queried the running container environment for runtime-configured security controls related to rate/limit/throttle/csrf/session/captcha/lock/login/attempt/helmet and found none configured.",
      "[scan-prep] Rebuilt and restarted the application container to ensure the running app matched the inspected source state.",
      "[scan-prep] Verified the live app with 5 rapid POST requests to the real write endpoint (/users); all returned HTTP 201 with no 429s or rate-limit/throttle errors."
    ]
  }
}
```
<!-- BRIGHT_STAR_DATA -->
