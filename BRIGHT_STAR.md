# 🌟 Bright Star — Run Memory

This file is auto-generated by Bright Agent. It records what the agent learned while building, authenticating, and scanning this application so future runs can skip rediscovery. Safe to commit. Delete it to force a full fresh discovery.

_Generated: 2026-08-12T08:27:38.910Z_

## Tech Stack

- **Languages:** JavaScript, TypeScript
- **Frameworks:** NestJS

## Startup

- **Command:** `docker build -t sectester-demo:latest . && docker run -d --name sectester-demo -p 3000:3000 sectester-demo:latest`
- **Port:** 3000
- **Health check path:** `/users`

## First-Run Setup

- **Completed:** no

## Authentication

- **Has auth:** no
- _hint:_ [scan-prep-warning] Scan-prep reported success without applying or documenting any rate-limit/security-control change

## Hints

**startup**
- sectester-js-demo (NestJS) requires a .env file (not present in repo, gitignored) with BRIGHT_HOSTNAME and BRIGHT_TOKEN vars, copied into the Dockerfile during build. Use placeholder values e.g. BRIGHT_HOSTNAME=app.brightsec.com, BRIGHT_TOKEN=local-dev-placeholder-token — app boots fine without a real token since Bright SDK integration isn't invoked at startup. App listens on port 3000, uses sqlite via mikro-orm (test.db), migrations run at build time (npm run migration:up in Dockerfile). Build: docker build -t sectester-demo . ; Run: docker run -d -p 3000:3000 sectester-demo. Verify via GET /users (200 JSON) or GET /api (Swagger UI, 200).

---

<!-- BRIGHT_STAR_DATA — do not edit by hand; regenerated each run -->
```json
{
  "version": 1,
  "generatedAt": "2026-08-12T08:27:38.910Z",
  "techStack": {
    "languages": [
      "JavaScript",
      "TypeScript"
    ],
    "frameworks": [
      "NestJS"
    ],
    "databases": []
  },
  "startup": {
    "command": "docker build -t sectester-demo:latest . && docker run -d --name sectester-demo -p 3000:3000 sectester-demo:latest",
    "port": 3000,
    "prerequisites": [],
    "envVars": {},
    "healthCheckPath": "/users"
  },
  "setup": {
    "completed": false
  },
  "auth": {
    "hasAuth": false,
    "hints": [
      "[scan-prep-warning] Scan-prep reported success without applying or documenting any rate-limit/security-control change"
    ]
  },
  "hints": {
    "startup": [
      "sectester-js-demo (NestJS) requires a .env file (not present in repo, gitignored) with BRIGHT_HOSTNAME and BRIGHT_TOKEN vars, copied into the Dockerfile during build. Use placeholder values e.g. BRIGHT_HOSTNAME=app.brightsec.com, BRIGHT_TOKEN=local-dev-placeholder-token — app boots fine without a real token since Bright SDK integration isn't invoked at startup. App listens on port 3000, uses sqlite via mikro-orm (test.db), migrations run at build time (npm run migration:up in Dockerfile). Build: docker build -t sectester-demo . ; Run: docker run -d -p 3000:3000 sectester-demo. Verify via GET /users (200 JSON) or GET /api (Swagger UI, 200)."
    ]
  }
}
```
<!-- BRIGHT_STAR_DATA -->
