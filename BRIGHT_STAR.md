# 🌟 Bright Star — Run Memory

This file is auto-generated by Bright Agent. It records what the agent learned while building, authenticating, and scanning this application so future runs can skip rediscovery. Safe to commit. Delete it to force a full fresh discovery.

_Generated: 2026-07-02T07:10:54.005Z_

## Tech Stack

- **Languages:** JavaScript, TypeScript
- **Frameworks:** NestJS

## Startup

- **Command:** `docker run --name sectester-js-demo-image -p 3000:3000 -d sectester-js-demo-image docker rm -f sectester-js-demo >/dev/null 2>&1 || true && docker rm -f sectester-js-demo-image >/dev/null 2>&1 || true && docker run --name sectester-js-demo -p 3000:3000 -e NODE_ENV=production -e BRIGHT_HOSTNAME=app.brightsec.com -e BRIGHT_TOKEN="$BRIGHT_TOKEN" -d sectester-js-demo-image`
- **Port:** 3000
- **Docker:** yes
- **Health check path:** `/api`
- **Prerequisites:**
  - `docker build -t sectester-js-demo-image .`
- **Environment variables:**
  - `BRIGHT_HOSTNAME=app.brightsec.com`
  - `BRIGHT_TOKEN=`
  - `NODE_ENV=production`

## First-Run Setup

- **Completed:** no

## Authentication

- **Has auth:** no
- _hint:_ [scan-prep] Scan-prep verification only observed HTTP 5xx on login POSTs; the login path is crashing, not verified as scanner-ready

---

<!-- BRIGHT_STAR_DATA — do not edit by hand; regenerated each run -->
```json
{
  "version": 1,
  "generatedAt": "2026-07-02T07:10:54.005Z",
  "techStack": {
    "languages": [
      "JavaScript",
      "TypeScript"
    ],
    "frameworks": [
      "NestJS"
    ],
    "databases": []
  },
  "startup": {
    "command": "docker run --name sectester-js-demo-image -p 3000:3000 -d sectester-js-demo-image docker rm -f sectester-js-demo >/dev/null 2>&1 || true && docker rm -f sectester-js-demo-image >/dev/null 2>&1 || true && docker run --name sectester-js-demo -p 3000:3000 -e NODE_ENV=production -e BRIGHT_HOSTNAME=app.brightsec.com -e BRIGHT_TOKEN=\"$BRIGHT_TOKEN\" -d sectester-js-demo-image",
    "port": 3000,
    "prerequisites": [
      "docker build -t sectester-js-demo-image ."
    ],
    "envVars": {
      "BRIGHT_HOSTNAME": "app.brightsec.com",
      "BRIGHT_TOKEN": "",
      "NODE_ENV": "production"
    },
    "docker": true,
    "healthCheckPath": "/api",
    "healthProbe": {
      "path": "/api",
      "method": "GET",
      "expectedStatuses": [
        200
      ]
    }
  },
  "setup": {
    "completed": false
  },
  "auth": {
    "hasAuth": false,
    "hints": [
      "[scan-prep] Scan-prep verification only observed HTTP 5xx on login POSTs; the login path is crashing, not verified as scanner-ready"
    ]
  }
}
```
<!-- BRIGHT_STAR_DATA -->
