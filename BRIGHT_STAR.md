# 🌟 Bright Star — Run Memory

This file is auto-generated by Bright Agent. It records what the agent learned while building, authenticating, and scanning this application so future runs can skip rediscovery. Safe to commit. Delete it to force a full fresh discovery.

_Generated: 2026-07-07T14:14:02.853Z_

## Tech Stack

- **Languages:** JavaScript, TypeScript
- **Frameworks:** NestJS

## Startup

- **Command:** `cp .env.example .env && docker build -t sectester-js-demo . && docker run --name sectester-js-demo -p 3000:3000 -d sectester-js-demo`
- **Port:** 3000
- **Docker:** yes
- **Health check path:** `/api`

## First-Run Setup

- **Completed:** no

## Authentication

- **Has auth:** no
- _hint:_ [scan-prep] The app is already permissive for DAST scanning; no blocking rate limits or similar security controls were present, and rapid-request verification succeeded.
- _hint:_ [scan-prep] Inspected NestJS application source, runtime environment, and containerized app for rate limiting, throttling, auth guards, CSRF enforcement, session controls, CAPTCHA, and other scanner-blocking protections.
- _hint:_ [scan-prep] Confirmed there is no framework-level or code-level rate limiter configured: no NestJS ThrottlerModule/ThrottlerGuard, no throttle decorators, no express-rate-limit, no auth/login lockout logic, no CAPTCHA, no CSRF middleware, no session middleware, and no IP allow/block lists.
- _hint:_ [scan-prep] Validated runtime container settings and installed dependencies to ensure no hidden rate-limit/security-control configuration was active in the running app.
- _hint:_ [scan-prep] Verified scanner-friendliness by sending 5 rapid POST requests to a real state-changing endpoint (/users); all returned HTTP 201 with no 429s or throttle-style error bodies.

---

<!-- BRIGHT_STAR_DATA — do not edit by hand; regenerated each run -->
```json
{
  "version": 1,
  "generatedAt": "2026-07-07T14:14:02.853Z",
  "techStack": {
    "languages": [
      "JavaScript",
      "TypeScript"
    ],
    "frameworks": [
      "NestJS"
    ],
    "databases": []
  },
  "startup": {
    "command": "cp .env.example .env && docker build -t sectester-js-demo . && docker run --name sectester-js-demo -p 3000:3000 -d sectester-js-demo",
    "port": 3000,
    "prerequisites": [],
    "envVars": {},
    "docker": true,
    "healthCheckPath": "/api"
  },
  "setup": {
    "completed": false
  },
  "auth": {
    "hasAuth": false,
    "hints": [
      "[scan-prep] The app is already permissive for DAST scanning; no blocking rate limits or similar security controls were present, and rapid-request verification succeeded.",
      "[scan-prep] Inspected NestJS application source, runtime environment, and containerized app for rate limiting, throttling, auth guards, CSRF enforcement, session controls, CAPTCHA, and other scanner-blocking protections.",
      "[scan-prep] Confirmed there is no framework-level or code-level rate limiter configured: no NestJS ThrottlerModule/ThrottlerGuard, no throttle decorators, no express-rate-limit, no auth/login lockout logic, no CAPTCHA, no CSRF middleware, no session middleware, and no IP allow/block lists.",
      "[scan-prep] Validated runtime container settings and installed dependencies to ensure no hidden rate-limit/security-control configuration was active in the running app.",
      "[scan-prep] Verified scanner-friendliness by sending 5 rapid POST requests to a real state-changing endpoint (/users); all returned HTTP 201 with no 429s or throttle-style error bodies."
    ]
  }
}
```
<!-- BRIGHT_STAR_DATA -->
